# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Environments
.env
venv/
.venv/
env/

# Models — all model artefacts are committed (total < 5 MB)
# Uncomment below to exclude large models if needed:
# models/*.joblib
# models/*.pkl

# Data — raw CSVs and processed features are committed for reproducibility
# SQLite local dev database is excluded (app auto-regenerates it)
data/retail_brain.db
data/cache/

# Logs & temp outputs
logs/
result.txt

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Streamlit secrets (never commit keys!) — config.toml IS committed
.streamlit/secrets.toml
